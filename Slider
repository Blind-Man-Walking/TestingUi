-- ===================================================================
-- CREATE SLIDER SCRIPT
-- Run this after creating a tab to add a slider to it.
-- ===================================================================

-- Find the window and the target tab created by the previous scripts
local Window = getgenv().Window
local TargetTab = getgenv().TargetTab -- This assumes the "Create Tab" script stored the tab here

-- Check if the necessary parts exist
if not Window or not TargetTab then
    warn("CreateSlider Error: Window or Target Tab not found. Please run the 'Create Window' and 'Create Tab' scripts first.")
    return
end

-- Create a new slider and add it to the target tab
local walkspeedSlider = TargetTab:AddSlider({
    Text = "Walkspeed",
    Min = 16,
    Max = 200,
    Default = 16
})

-- Connect a function to the slider to do something when its value changes
walkspeedSlider.State.Changed:Connect(function(newValue)
    print("Walkspeed slider is now set to:", newValue)
    
    -- Example of how you would use this to change the player's speed:
    -- local player = game:GetService("Players").LocalPlayer
    -- if player.Character and player.Character:FindFirstChild("Humanoid") then
    --     player.Character.Humanoid.WalkSpeed = newValue
    -- end
end)

print("Slider 'Walkspeed' was added to the target tab successfully.")
