-- ===================================================================
-- CREATE MAIN TAB SCRIPT
-- Run this after creating the window to add the main welcome tab.
-- ===================================================================

-- Find the window created by the previous script
local Window = getgenv().Window

-- Check if the window exists
if not Window then
    warn("CreateMainTab Error: The main window was not found. Please run the 'Create Window' script first.")
    return
end

-- Create the "Main" tab
local mainTab = Window:AddTab("Main")

-- Get the local player to display their info
local Players = game:GetService("Players")
local player = Players.LocalPlayer

-- Add a simple welcome label first
mainTab:AddLabel("Welcome, " .. player.DisplayName .. "!")

-- Add a button for a common action, like copying a Discord link
local discordButton = mainTab:AddButton("Copy Discord Link")
discordButton.MouseButton1Click:Connect(function()
    -- Make sure you have the 'setclipboard' function available in your executor
    if setclipboard then
        setclipboard("https://your.discord.link/here")
        print("Discord link copied to clipboard!")
    else
        print("Clipboard function not available.")
    end
end)

-- You can add more detailed info frames or player thumbnails here if you expand your library
-- For now, this creates a functional Main tab.

print("Tab 'Main' was created successfully.")
