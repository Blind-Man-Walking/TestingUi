-- ===================================================================
-- RUN KEY SYSTEM SCRIPT
-- This script loads your library and starts the key system.
-- ===================================================================

-- The URL to your main UI library on GitHub
local LIBRARY_URL = "https://raw.githubusercontent.com/Blind-Man-Walking/TestingUi/refs/heads/main/Test"

-- Load the library
local MyLibrary = loadstring(game:HttpGet(LIBRARY_URL))()

-- Check if the library loaded correctly
if not MyLibrary then
    warn("KeySystem Error: Could not load the main UI Library from GitHub.")
    return
end

-- This function defines what will happen AFTER you enter a correct key.
-- In a real script, this is where you would run the "Create Window" script.
local function onKeySuccess()
    print("Key Accepted! You can now load the main UI.")
    
    -- Example: You could now create the window automatically
    -- getgenv().Window = MyLibrary.CreateWindow({ Title = "|   My Hub   |" })
    -- print("Window created successfully.")
end

-- Start the key system and tell it to run our function on success
MyLibrary.CreateKeySystem(onKeySuccess)

print("Key System is now running. Please enter a valid key.")
